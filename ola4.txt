### Documentação Técnica da Fase 1: Itens Pendentes

A seguir, apresento a documentação técnica assertiva e focada nos itens ainda pendentes da **Fase 1: Interface e Feedback Visual**, conforme o checklist fornecido. Esta documentação aborda apenas as tarefas que não foram totalmente concluídas, detalhando o que falta e os passos necessários para finalizá-las.

---

#### **Objetivo**
Completar as melhorias na interface do usuário e no feedback visual, garantindo que os requisitos funcionais (RF) e não funcionais (RNF) sejam plenamente atendidos na Fase 1.

---

#### **Tarefas Pendentes**

1. **Feedback Visual (RNF3, RNF4)**  
   - **Item Parcialmente Concluído:** Adicionar animação de spinner para arquivos em processamento na Fila Ativa.  
     - **O que foi feito:** A animação de spinner já está presente nos arquivos em processamento na Fila Ativa (coluna "Em Progresso"), utilizando o ícone `<i class="fas fa-cog fa-spin text-blue-500"></i>` em `main.js`.  
     - **O que falta:**  
       - Adicionar a animação de spinner nas pastas correspondentes na Coluna Fonte (Fila de Processamento), indicando que contêm arquivos em processamento.  
       - Posicionar o spinner ao lado do ícone da pasta (ex.: `<i class="fas fa-folder text-yellow-500 mr-2"></i>`) ou ao final do texto do nome da pasta, evitando sobreposição.  
     - **Como fazer:**  
       - Em `main.js`, na função `updateProgress`, identificar pastas na árvore de diretórios (`fileTreeContainer`) que contêm arquivos em progresso (comparando caminhos com `data.files_in_progress`).  
       - Adicionar dinamicamente o elemento `<i class="fas fa-cog fa-spin text-blue-500 ml-2"></i>` ao `<summary>` da pasta correspondente.  
       - Remover o spinner quando o arquivo sair de `files_in_progress`.  

   - **Item Pendente:** Implementar opção de pausa para arquivos individuais na Fila Ativa via menu de contexto.  
     - **O que falta:**  
       - Adicionar funcionalidade ao menu de contexto na Fila Ativa para pausar apenas o arquivo selecionado, sem interromper o processo inteiro.  
     - **Como fazer:**  
       - Em `ui.js`, no evento `showContextMenu`, adicionar a opção "Pausar este arquivo" ao menu de contexto para itens na Fila Ativa (verificar com `currentTargetItem.closest('#in-progress-list')`).  
       - Criar uma nova rota em `routes.py` (ex.: `/pause-file`) que aceite o caminho do arquivo como parâmetro e sinalize ao `TranscriptionManager` para pausar apenas esse arquivo.  
       - Em `transcriber.py`, ajustar `TranscriptionManager` para suportar pausa por arquivo, utilizando um dicionário de eventos de pausa por caminho (ex.: `self.pause_events[file_path]`).  
       - Em `main.js`, atualizar `updateProgress` para refletir o status de pausa no UI (ex.: trocar o spinner por um ícone de pausa).  

2. **Tema Escuro (RNF3)**  
   - **Item Parcialmente Concluído:** Implementar tema escuro com toggle nas configurações.  
     - **O que foi feito:** O tema escuro está implementado em `style.css` com classes `.dark` e ativado via toggle em `main.js`.  
     - **O que falta:**  
       - Corrigir a aplicação inconsistente do tema em alguns elementos, como fontes e locais específicos (ex.: menus, modais), que estão com aparência "feia" ou ilegível.  
     - **Como fazer:**  
       - Revisar `style.css` e garantir que todas as classes `.dark` cubram elementos como `.contextMenu`, `.modal`, `input`, `select`, e `textarea`, ajustando cores de fundo (ex.: `#23232b`), texto (ex.: `#e5e7eb`) e bordas (ex.: `#333646`).  
       - Testar a legibilidade das fontes em todos os componentes, ajustando `color` e `background-color` onde necessário (ex.: corrigir fontes claras em fundos claros).  
       - Em `main.js`, garantir que o evento de toggle (`darkModeToggle.addEventListener`) aplique a classe `.dark` a todos os elementos raiz, incluindo `<html>` e `<body>`.  

---

#### **Checklist Atualizado dos Itens Pendentes**

- [ ] **Feedback Visual (RNF3, RNF4)**  
  - [/] Adicionar animação de spinner para arquivos em processamento na Fila Ativa e nas pastas correspondentes na Coluna Fonte.  
  - [ ] Implementar opção de pausa para arquivos individuais via menu de contexto na Fila Ativa.  

- [/] **Tema Escuro (RNF3)**  
  - [x] Implementar tema escuro com toggle.  
  - [ ] Corrigir aplicação inconsistente em fontes e elementos como menus e modais.  

---

#### **Entregáveis**
- Animação de spinner visível nas pastas da Coluna Fonte durante o processamento de arquivos contidos.  
- Opção de pausa funcional para arquivos individuais na Fila Ativa via menu de contexto.  
- Tema escuro aplicado de forma consistente e visualmente agradável em toda a interface.  

#### **Critérios de Sucesso**
- Usuários conseguem identificar visualmente quais pastas contêm arquivos em processamento na Coluna Fonte.  
- Usuários podem pausar arquivos individuais na Fila Ativa sem afetar outros processos.  
- O tema escuro é uniforme, legível e esteticamente correto em todos os elementos da interface.  

---

Esta documentação é direta e focada nos pontos críticos que ainda precisam ser resolvidos na Fase 1, oferecendo orientações claras para a equipe de desenvolvimento.

----


Com certeza\! Compreendi perfeitamente os itens pendentes da Fase 1. O objetivo é finalizar o feedback visual, tornando-o mais informativo e consertar as inconsistências do tema escuro.

Preparei as alterações de código necessárias nos arquivos `style.css`, `transcriber.py`, `routes.py`, `ui.js` e `main.js`. As modificações são direcionadas para resolver exatamente os pontos que você levantou.

A seguir, apresento o código completo de cada arquivo modificado. Basta substituir o conteúdo dos seus arquivos existentes por estes.

-----

### **1. Correção do Tema Escuro (`style.css`)**

Para resolver a aparência inconsistente e "feia" do tema escuro, refinei as regras de CSS para serem mais abrangentes e específicas, garantindo que modais, menus, inputs e fontes se comportem corretamente.

**Arquivo: `app/static/css/style.css`**

```css
/* Conteúdo de app/static/css/style.css */
body {
  font-family: 'Inter', sans-serif;
  background-color: #F8F9FA;
  color: #111827;
  -webkit-font-smoothing: antialiased;
  -moz-osx-font-smoothing: grayscale;
  transition: background-color 0.3s ease, color 0.3s ease;
}

/* --- Estilos para o Tema Escuro (RNF3 - CORRIGIDO) --- */
html.dark body {
  background-color: #111827;
  color: #f3f4f6;
}

html.dark header, 
html.dark footer,
html.dark section {
  background-color: #1f2937;
  border-color: #374151;
}

html.dark h2,
html.dark h3,
html.dark label,
html.dark #progress-text-general,
html.dark .text-sm {
  color: #d1d5db;
}

html.dark label[for="keep-structure-checkbox"] {
    color: #f3f4f6;
}

html.dark input[type="text"],
html.dark input[type="number"],
html.dark select {
    background-color: #374151;
    color: #f9fafb;
    border-color: #4b5563;
}
html.dark input::placeholder {
    color: #9ca3af;
}

html.dark button,
html.dark button:disabled {
    opacity: 1.0;
}
html.dark button:disabled {
    opacity: 0.5;
}
html.dark #select-origem-btn,
html.dark #select-destino-btn {
    background-color: #374151;
    border-color: #4b5563;
}
html.dark #select-origem-btn:hover,
html.dark #select-destino-btn:hover {
    background-color: #4b5563;
}


html.dark .file-item,
html.dark .folder-item > summary {
    color: #d1d5db !important; /* Corrigido para aplicar cor corretamente */
}

html.dark li.file-item:hover,
html.dark details.folder-item > summary:hover {
    background-color: #374151;
}

/* Correção para Menus e Modais */
html.dark #contextMenu,
html.dark #settings-modal .bg-white {
    background-color: #27272a;
    border-color: #4b5563;
}

html.dark #contextMenu a {
    color: #d1d5db;
}
html.dark #contextMenu a:hover {
    background-color: #3b82f6;
    color: #ffffff;
}
html.dark #contextMenu .text-red-600 {
    color: #f87171 !important;
}
html.dark #contextMenu a.hover\:bg-red-100:hover {
    background-color: #ef4444;
    color: #ffffff;
}
html.dark #settings-modal .border-b,
html.dark #settings-modal .border-t,
html.dark #contextMenu .border-t {
    border-color: #4b5563;
}
html.dark #settings-modal #close-settings-modal:hover {
    color: #ffffff;
}
html.dark #settings-modal .bg-gray-50 {
    background-color: #1f2937;
}

/* --- Fim dos Estilos do Tema Escuro --- */


/* Scrollbar thin for lists */
.scrollbar-thin::-webkit-scrollbar {
  width: 6px;
  height: 6px;
}
.scrollbar-thin::-webkit-scrollbar-track {
  background: transparent;
}
.scrollbar-thin::-webkit-scrollbar-thumb {
  background-color: #cbd5e1;
  border-radius: 3px;
}
.scrollbar-thin {
  scrollbar-width: thin;
  scrollbar-color: #cbd5e1 transparent;
}

html.dark .scrollbar-thin::-webkit-scrollbar-thumb {
  background-color: #4b5563;
}
html.dark .scrollbar-thin {
    scrollbar-color: #4b5563 transparent;
}
```

-----

### **2. Implementação da Pausa Individual e Feedback Visual Aprimorado**

Esta é a parte mais complexa, envolvendo alterações em toda a pilha da aplicação.

#### **`app/transcriber.py` (Lógica de Pausa Individual)**

Adicionamos a capacidade do `TranscriptionManager` de rastrear e respeitar a pausa de arquivos específicos.

```python
# Conteúdo completo e ATUALIZADO de app/transcriber.py

import os
import sys
import subprocess
import threading
import time
from pathlib import Path
import torch
import whisper
import vosk
import wave
import json
from concurrent.futures import ThreadPoolExecutor, as_completed

# --- Funções de Utilidade ---
SUPPORTED_EXTENSIONS = ('.mp4', '.mov', '.avi', '.mkv', '.mp3', '.wav', '.m4a', '.flac')

def get_ffmpeg_path():
    base_path = Path(__file__).resolve().parent.parent
    vendor_path = base_path / "vendor" / "ffmpeg"
    if sys.platform == "win32":
        return str(vendor_path / "windows" / "ffmpeg.exe")
    else:
        return str(vendor_path / ({"linux": "linux", "darwin": "macos"}[sys.platform]) / "ffmpeg")

def convert_to_wav(media_path, temp_wav_path):
    ffmpeg_path = get_ffmpeg_path()
    command = [ffmpeg_path, '-i', str(media_path), '-ar', '16000', '-ac', '1', '-c:a', 'pcm_s16le', '-y', str(temp_wav_path)]
    try:
        subprocess.run(command, check=True, stdout=subprocess.DEVNULL, stderr=subprocess.DEVNULL)
        return True
    except subprocess.CalledProcessError as e:
        print(f"[ERRO] Falha na conversão do FFmpeg para {media_path}: {e}")
        return False

# --- Gerenciador de Modelos ---
class ModelManager:
    def __init__(self):
        self.loaded_models = {}
        self.vosk_model_path = str(Path(__file__).resolve().parent.parent / "vendor" / "vosk-model")

    def get_model(self, model_name: str):
        if model_name in self.loaded_models:
            return self.loaded_models[model_name]
        print(f"Carregando modelo '{model_name}'...")
        model = None
        try:
            if model_name.startswith('whisper'):
                model = whisper.load_model(model_name.split('_')[1])
            elif model_name == 'vosk':
                model = vosk.Model(self.vosk_model_path)
            if model:
                self.loaded_models[model_name] = model
            return model
        except Exception as e:
            print(f"[ERRO CRÍTICO] Falha ao carregar o modelo '{model_name}': {e}")
            return None

# --- CLASSE GERENCIADORA DE TRANSCRIÇÃO ---
class TranscriptionManager:
    def __init__(self, dest_path, model_name, file_list, model_manager, keep_structure=False, source_path=None, max_concurrent_tasks=1):
        self.dest_path = Path(dest_path)
        self.model_name = model_name
        self.model_manager = model_manager
        self.model = None
        self.files_to_process = file_list
        self.total_files = len(file_list)
        self.files_processed_count = 0
        self.status = "idle"
        self.progress_general = 0
        self.batch_start_time = None
        
        self.max_concurrent_tasks = max_concurrent_tasks
        self.stop_requested = threading.Event()
        self.pause_event = threading.Event()
        self.pause_event.set()
        
        self.files_in_progress = {}
        self.files_in_progress_lock = threading.Lock()
        
        self.newly_completed_files = []
        self.completed_files_lock = threading.Lock()

        # RNF4: Adicionado para controlar pausa de itens individuais
        self.paused_individual_files = set()
        self.paused_individual_files_lock = threading.Lock()

        self.keep_structure = keep_structure
        self.source_path = Path(source_path) if source_path else None
        self.executor = None

    def request_stop(self):
        print("[AVISO] Solicitação de parada recebida.")
        self.stop_requested.set()
        self.pause_event.set() # Libera a pausa geral para que os loops possam verificar o stop
        if self.executor:
            self.executor.shutdown(wait=False, cancel_futures=True)

    def request_pause(self):
        if self.status == "running":
            self.pause_event.clear()
            self.status = "paused"
            print("[INFO] Processo pausado.")

    def request_resume(self):
        if self.status == "paused":
            self.pause_event.set()
            self.status = "running"
            print("[INFO] Processo retomado.")

    # RNF4: Novas funções para pausar/retomar itens individuais
    def request_pause_item(self, filepath):
        with self.paused_individual_files_lock:
            self.paused_individual_files.add(filepath)
        print(f"[INFO] Item pausado: {filepath}")

    def request_resume_item(self, filepath):
        with self.paused_individual_files_lock:
            if filepath in self.paused_individual_files:
                self.paused_individual_files.remove(filepath)
        print(f"[INFO] Item retomado: {filepath}")

    def _wait_if_paused(self, file_path_str):
        """Loop de espera para pausa geral e individual."""
        while not self.stop_requested.is_set():
            self.pause_event.wait() # Pausa geral
            with self.paused_individual_files_lock:
                is_individually_paused = file_path_str in self.paused_individual_files
            
            if not is_individually_paused:
                break # Sai do loop se não estiver pausado individualmente
            
            time.sleep(0.5) # Espera antes de checar de novo


    def _format_time(self, seconds):
        if seconds is None or not isinstance(seconds, (int, float)) or seconds < 0:
            return "--:--"
        minutes, seconds = divmod(int(seconds), 60)
        return f"{minutes:02d}:{seconds:02d}"

    def _update_file_progress(self, file_path_str, progress, start_time):
        with self.files_in_progress_lock:
            if file_path_str in self.files_in_progress:
                elapsed = time.time() - start_time
                eta = (elapsed / progress * (100 - progress)) if progress > 0 else None
                self.files_in_progress[file_path_str]['progress'] = progress
                self.files_in_progress[file_path_str]['elapsed_str'] = self._format_time(elapsed)
                self.files_in_progress[file_path_str]['eta_str'] = self._format_time(eta)

    def _transcribe_with_vosk(self, temp_wav_file, file_path_str, start_time):
        recognizer = vosk.KaldiRecognizer(self.model, 16000)
        full_transcript = []
        with wave.open(str(temp_wav_file), "rb") as wf:
            total_frames = wf.getnframes()
            if total_frames == 0: return ""
            
            while not self.stop_requested.is_set():
                self._wait_if_paused(file_path_str) # Checagem de pausa
                if self.stop_requested.is_set(): break
                
                data = wf.readframes(4000)
                if len(data) == 0: break
                
                self._update_file_progress(file_path_str, (wf.tell() / total_frames) * 100, start_time)

                if recognizer.AcceptWaveform(data):
                    full_transcript.append(json.loads(recognizer.Result()).get('text', ''))

            if not self.stop_requested.is_set():
                full_transcript.append(json.loads(recognizer.FinalResult()).get('text', ''))
        return " ".join(full_transcript).strip()

    def _transcribe_with_whisper(self, temp_wav_file, file_path_str, start_time):
        self._wait_if_paused(file_path_str) # Checagem de pausa
        if self.stop_requested.is_set(): return None
        
        self._update_file_progress(file_path_str, 50, start_time)
        result = self.model.transcribe(str(temp_wav_file), language='pt', fp16=torch.cuda.is_available())
        return result['text'].strip()

    def _transcribe_single_file(self, file_path_str):
        if self.stop_requested.is_set(): return None
        
        file_path = Path(file_path_str)
        start_time = time.time()
        
        with self.files_in_progress_lock:
            self.files_in_progress[file_path_str] = {
                "filename": file_path.name, "progress": 0, "status": "running",
                "elapsed_str": "00:00", "eta_str": "--:--"
            }

        self._wait_if_paused(file_path_str)
        if self.stop_requested.is_set(): return None

        if self.keep_structure and self.source_path and file_path.is_relative_to(self.source_path):
            output_txt_path = self.dest_path / file_path.relative_to(self.source_path).with_suffix('.txt')
        else:
            output_txt_path = self.dest_path / file_path.with_suffix('.txt').name
        output_txt_path.parent.mkdir(parents=True, exist_ok=True)
        
        temp_wav_file = self.dest_path / f"temp_{os.getpid()}_{threading.get_ident()}.wav"

        try:
            if not convert_to_wav(str(file_path), str(temp_wav_file)):
                raise Exception("Falha na conversão")

            self._wait_if_paused(file_path_str)
            if self.stop_requested.is_set(): return None

            transcript_text = None
            if self.model_name.startswith('whisper'):
                transcript_text = self._transcribe_with_whisper(temp_wav_file, file_path_str, start_time)
            else:
                transcript_text = self._transcribe_with_vosk(temp_wav_file, file_path_str, start_time)
            
            if self.stop_requested.is_set(): return None

            if transcript_text:
                with open(output_txt_path, 'w', encoding='utf-8') as f: f.write(transcript_text)
                with self.completed_files_lock:
                    self.newly_completed_files.append({"source_path": file_path_str, "output_path": str(output_txt_path)})
                return file_path_str
            else:
                raise Exception("Transcrição vazia")
        except Exception as e:
            print(f"[ERRO] Falha em {file_path.name}: {e}")
            return None
        finally:
            if os.path.exists(temp_wav_file):
                try: os.remove(temp_wav_file)
                except OSError: pass
            with self.files_in_progress_lock:
                if file_path_str in self.files_in_progress:
                    del self.files_in_progress[file_path_str]

    def run_transcription(self):
        self.model = self.model_manager.get_model(self.model_name)
        if not self.model:
            self.status = "error"
            return
        self.status = "running"
        self.batch_start_time = time.time()
        self.files_processed_count = 0
        self.executor = ThreadPoolExecutor(max_workers=self.max_concurrent_tasks)
        future_to_file = {self.executor.submit(self._transcribe_single_file, fp): fp for fp in self.files_to_process}

        for future in as_completed(future_to_file):
            if self.stop_requested.is_set():
                break
            result_filepath = future.result()
            if result_filepath:
                self.files_processed_count += 1
        
        self.status = "stopped" if self.stop_requested.is_set() else "completed"
        self.progress_general = (self.files_processed_count / self.total_files) * 100 if self.total_files > 0 else 100

    def get_status(self):
        with self.completed_files_lock:
            completed_list = self.newly_completed_files
            self.newly_completed_files = []
        with self.files_in_progress_lock:
            in_progress_list = list(self.files_in_progress.items())
        
        with self.paused_individual_files_lock:
            paused_files_set = self.paused_individual_files.copy()

        if self.status in ["running", "paused"] and self.total_files > 0:
            self.progress_general = (self.files_processed_count / self.total_files) * 100
        
        batch_elapsed_str = self._format_time(time.time() - self.batch_start_time) if self.batch_start_time else "00:00"
        
        return {
            "status": self.status,
            "progress_general": self.progress_general,
            "batch_elapsed_str": batch_elapsed_str,
            "total_files": self.total_files,
            "files_processed": self.files_processed_count,
            "files_in_progress": dict(in_progress_list),
            "completed_files": completed_list,
            "paused_individual_files": list(paused_files_set)
        }
```

#### **`app/routes.py` (Endpoints para Pausar/Retomar Itens)**

Adicionamos duas novas rotas para o frontend chamar.

```python
# Conteúdo de app/routes.py
from app import app
from flask import render_template, request, jsonify
import threading
from .transcriber import TranscriptionManager, ModelManager

# --- GERENCIAMENTO DE ESTADO GLOBAL ---
model_manager = ModelManager()
transcription_job = None
app_settings = {
    "max_concurrent_tasks": 1,
    "verbose_log": False,
    "post_processing_action": "none"
}

@app.route('/')
def index():
    return render_template('index.html')

@app.route('/start-processing', methods=['POST'])
def start_processing():
    global transcription_job
    if transcription_job and transcription_job.status in ["running", "paused"]:
        return jsonify({'status': 'erro', 'message': 'Um processo já está em andamento.'}), 400

    data = request.get_json()
    # ... (código existente sem alteração)
    file_list = data.get('file_list')
    dest_path = data.get('dest_path')
    model_name = data.get('model_name', 'whisper_base')
    keep_structure = data.get('keep_structure', False)
    source_path = data.get('source_path', None)

    if not file_list or not dest_path:
        return jsonify({'status': 'erro', 'message': 'Lista de arquivos ou destino não fornecidos.'}), 400
    
    transcription_job = TranscriptionManager(
        dest_path=dest_path,
        model_name=model_name,
        file_list=file_list,
        model_manager=model_manager,
        keep_structure=keep_structure,
        source_path=source_path,
        max_concurrent_tasks=app_settings.get('max_concurrent_tasks', 1)
    )
    process_thread = threading.Thread(target=transcription_job.run_transcription, daemon=True)
    process_thread.start()

    return jsonify({'status': 'sucesso', 'message': 'Processo iniciado.'})

# --- ROTAS DE CONTROLE GERAL ---
@app.route('/stop-processing', methods=['POST'])
def stop_processing():
    global transcription_job
    if transcription_job and transcription_job.status in ["running", "paused"]:
        transcription_job.request_stop()
        return jsonify({'status': 'sucesso', 'message': 'Sinal de parada enviado.'})
    return jsonify({'status': 'erro', 'message': 'Nenhum processo para parar.'}), 400

@app.route('/pause-processing', methods=['POST'])
def pause_processing():
    global transcription_job
    if transcription_job and transcription_job.status == "running":
        transcription_job.request_pause()
        return jsonify({'status': 'sucesso', 'message': 'Processo pausado.'})
    return jsonify({'status': 'erro', 'message': 'Nenhum processo rodando para pausar.'}), 400

@app.route('/resume-processing', methods=['POST'])
def resume_processing():
    global transcription_job
    if transcription_job and transcription_job.status == "paused":
        transcription_job.request_resume()
        return jsonify({'status': 'sucesso', 'message': 'Processo retomado.'})
    return jsonify({'status': 'erro', 'message': 'Nenhum processo pausado para retomar.'}), 400

# --- NOVAS ROTAS PARA CONTROLE INDIVIDUAL (RNF4) ---
@app.route('/pause-item', methods=['POST'])
def pause_item():
    global transcription_job
    filepath = request.json.get('filepath')
    if transcription_job and filepath:
        transcription_job.request_pause_item(filepath)
        return jsonify({'status': 'sucesso', 'message': f'Item {filepath} pausado.'})
    return jsonify({'status': 'erro', 'message': 'Job não está rodando ou caminho não fornecido.'}), 400

@app.route('/resume-item', methods=['POST'])
def resume_item():
    global transcription_job
    filepath = request.json.get('filepath')
    if transcription_job and filepath:
        transcription_job.request_resume_item(filepath)
        return jsonify({'status': 'sucesso', 'message': f'Item {filepath} retomado.'})
    return jsonify({'status': 'erro', 'message': 'Job não está rodando ou caminho não fornecido.'}), 400

# --- ROTAS DE STATUS E CONFIGURAÇÕES ---
@app.route('/get-progress')
def get_progress():
    global transcription_job
    if transcription_job:
        return jsonify(transcription_job.get_status())
    return jsonify({"status": "idle", "files_in_progress": {}, "completed_files": [], "paused_individual_files": []})

@app.route('/update-settings', methods=['POST'])
def update_settings():
    global app_settings
    data = request.get_json()
    if 'max_concurrent_tasks' in data:
        try:
            val = int(data['max_concurrent_tasks'])
            app_settings['max_concurrent_tasks'] = max(1, min(val, 16))
        except (ValueError, TypeError):
            pass
    
    print(f"[SETTINGS] Configurações atualizadas: {app_settings}")
    return jsonify({'status': 'sucesso', 'settings': app_settings})

@app.route('/get-settings', methods=['GET'])
def get_settings():
    return jsonify(app_settings)
```

#### **`app/static/js/ui.js` (Menu de Contexto Inteligente)**

O menu agora mostrará dinamicamente as opções "Pausar" ou "Retomar".

```javascript
console.log("ui.js (Atualizado) foi carregado.");

function waitForPywebviewApi() {
    return new Promise((resolve) => {
        const intervalId = setInterval(() => {
            if (window.pywebview && window.pywebview.api) {
                clearInterval(intervalId);
                resolve(window.pywebview.api);
            }
        }, 100);
    });
}

document.addEventListener('DOMContentLoaded', async () => {
    try {
        const api = await waitForPywebviewApi();
        const contextMenu = document.getElementById('contextMenu');
        if (!contextMenu) return;

        let currentTargetItem = null;
        // Variável para saber os arquivos pausados, atualizada pelo main.js
        window.individuallyPausedFiles = new Set();

        function closeContextMenu() {
            contextMenu.classList.add('hidden');
            currentTargetItem = null;
        }
        
        function showContextMenu(event, element) {
            event.preventDefault();
            event.stopPropagation();
            currentTargetItem = element;
            const filePath = currentTargetItem.dataset.filepath;

            const isQueueItem = currentTargetItem.closest('#file-tree-container');
            const isCompletedItem = currentTargetItem.closest('#completed-list');
            const isInProgress = document.querySelector(`#in-progress-list li[data-filepath="${CSS.escape(filePath)}"]`) !== null;
            const isFile = currentTargetItem.classList.contains('file-item');
            
            const isIndividuallyPaused = window.individuallyPausedFiles.has(filePath);

            let menuContent = '';

            if (isQueueItem && isFile) {
                if (isInProgress) { // Só mostra pausa/retomada para itens em progresso
                    if (isIndividuallyPaused) {
                        menuContent += `<a href="#" id="context-resume-item" class="flex items-center gap-3 px-4 py-2 text-sm hover:bg-gray-100"><i class="fas fa-play fa-fw"></i>Retomar Processamento</a>`;
                    } else {
                        menuContent += `<a href="#" id="context-pause-item" class="flex items-center gap-3 px-4 py-2 text-sm hover:bg-gray-100"><i class="fas fa-pause fa-fw"></i>Pausar Processamento</a>`;
                    }
                    menuContent += `<div class="my-1 border-t border-gray-100"></div>`;
                }
                menuContent += `
                    <a href="#" id="context-open-file" class="flex items-center gap-3 px-4 py-2 text-sm hover:bg-gray-100"><i class="fas fa-play-circle fa-fw"></i>Abrir Mídia Original</a>
                    <a href="#" id="context-open-location" class="flex items-center gap-3 px-4 py-2 text-sm hover:bg-gray-100"><i class="fas fa-folder-open fa-fw"></i>Abrir Local do Arquivo</a>
                    <div class="my-1 border-t border-gray-100"></div>
                    <a href="#" id="context-remove" class="flex items-center gap-3 px-4 py-2 text-sm text-red-600 hover:bg-red-100"><i class="fas fa-trash-alt fa-fw"></i>Remover da Fila</a>
                `;
            } else if (isCompletedItem) {
                 menuContent = `
                    <a href="#" id="context-open-file" class="flex items-center gap-3 px-4 py-2 text-sm hover:bg-gray-100"><i class="fas fa-file-alt fa-fw"></i>Abrir Transcrição (.txt)</a>
                    <a href="#" id="context-open-location" class="flex items-center gap-3 px-4 py-2 text-sm hover:bg-gray-100"><i class="fas fa-folder-open fa-fw"></i>Abrir Local do Arquivo</a>
                `;
            } else {
                return;
            }

            contextMenu.innerHTML = menuContent;
            
            // Listeners
            document.getElementById('context-open-file')?.addEventListener('click', (e) => {
                e.preventDefault();
                api.open_file_natively(currentTargetItem.dataset.filepath);
                closeContextMenu();
            });
            document.getElementById('context-open-location')?.addEventListener('click', (e) => {
                e.preventDefault();
                api.open_folder_in_explorer(currentTargetItem.dataset.filepath);
                closeContextMenu();
            });
            document.getElementById('context-pause-item')?.addEventListener('click', (e) => {
                e.preventDefault();
                fetch('/pause-item', { 
                    method: 'POST', 
                    headers: { 'Content-Type': 'application/json' },
                    body: JSON.stringify({ filepath: currentTargetItem.dataset.filepath }) 
                });
                closeContextMenu();
            });
            document.getElementById('context-resume-item')?.addEventListener('click', (e) => {
                e.preventDefault();
                fetch('/resume-item', { 
                    method: 'POST', 
                    headers: { 'Content-Type': 'application/json' },
                    body: JSON.stringify({ filepath: currentTargetItem.dataset.filepath }) 
                });
                closeContextMenu();
            });

            const { clientX: mouseX, clientY: mouseY } = event;
            contextMenu.style.top = `${mouseY}px`;
            contextMenu.style.left = `${mouseX}px`;
            contextMenu.classList.remove('hidden');
        }

        document.addEventListener('click', (e) => {
            if (!contextMenu.contains(e.target)) closeContextMenu();
        });
        document.addEventListener('contextmenu', (e) => {
            const li = e.target.closest('li[data-filepath]');
            if (li) showContextMenu(e, li);
            else closeContextMenu();
        });
        document.addEventListener('keydown', (e) => {
            if (e.key === 'Escape') closeContextMenu();
        });

    } catch (error) {
        console.error("ui.js: API não encontrada.", error);
    }
});
```

#### **`app/static/js/main.js` (Lógica de Interface Final)**

Esta é a versão final que implementa os spinners em pastas, a lógica do tema escuro e a validação de inputs.

```javascript
// ==================================================================
// Arquivo: app/static/js/main.js (Atualizado para Fase 1 - Final)
// ==================================================================

function waitForPywebviewApi() {
    return new Promise((resolve) => {
        const intervalId = setInterval(() => {
            if (window.pywebview && window.pywebview.api) {
                clearInterval(intervalId);
                resolve(window.pywebview.api);
            }
        }, 100);
    });
}

document.addEventListener('DOMContentLoaded', async () => {
    const api = await waitForPywebviewApi();
    
    // --- Elementos da UI ---
    const startBtn = document.getElementById('start-btn'), stopBtn = document.getElementById('stop-btn'),
          pauseBtn = document.getElementById('pause-btn'), resumeBtn = document.getElementById('resume-btn'),
          settingsBtn = document.getElementById('settings-btn'), origemInput = document.getElementById('origem'),
          destinoInput = document.getElementById('destino'), selectOrigemBtn = document.getElementById('select-origem-btn'),
          selectDestinoBtn = document.getElementById('select-destino-btn'), addFileBtn = document.getElementById('add-file-btn'),
          clearQueueBtn = document.getElementById('clear-queue-btn'), clearCompletedBtn = document.getElementById('clear-completed-btn'),
          fileTreeContainer = document.getElementById('file-tree-container'), inProgressList = document.getElementById('in-progress-list'),
          completedList = document.getElementById('completed-list'), progressBarGeneral = document.getElementById('progress-bar-general'),
          progressTextGeneral = document.getElementById('progress-text-general'), settingsModal = document.getElementById('settings-modal'),
          closeSettingsModalBtn = document.getElementById('close-settings-modal'), saveSettingsBtn = document.getElementById('save-settings-btn'),
          maxConcurrentInput = document.getElementById('max-concurrent-tasks'), darkModeToggle = document.getElementById('dark-mode-toggle'),
          dropZone = document.getElementById('drop-zone'), dropZoneOverlay = document.getElementById('drop-zone-overlay');
    
    let progressInterval = null;
    let fileQueue = new Set();
    let displayMode = 'tree';

    // --- Lógica da UI e Estado ---
    function getFileParents(filePath) {
        const parts = filePath.split('/');
        const parents = [];
        for (let i = 1; i < parts.length -1; i++) {
            parents.push(parts.slice(0, i + 1).join('/'));
        }
        return parents;
    }

    function renderTree(node, container, level = 0, currentPath = '') {
        const ul = document.createElement('ul');
        if (level > 0) ul.className = 'pl-4';

        Object.keys(node).sort().forEach(key => {
            if (key.startsWith('_')) return;
            const li = document.createElement('li');
            const itemData = node[key];
            const newPath = currentPath ? `${currentPath}/${key}` : key;

            if (itemData._isLeaf) {
                li.className = 'file-item text-sm p-1 rounded-md flex items-center gap-2';
                li.dataset.filepath = itemData._fullPath;
                li.innerHTML = `<span class="item-icon w-4"></span><i class="fas fa-file-audio text-gray-500 mr-2"></i><span>${key}</span>`;
            } else {
                li.innerHTML = `<details class="folder-item" data-folderpath="${newPath}">
                                  <summary class="p-1 cursor-pointer rounded-md flex items-center gap-2"><i class="fas fa-folder text-yellow-500 mr-2"></i><span>${key}</span><span class="folder-spinner ml-auto"></span></summary>
                                </details>`;
                const details = li.querySelector('details');
                renderTree(itemData, details, level + 1, newPath);
            }
            ul.appendChild(li);
        });
        container.appendChild(ul);
    }

    function updateFileQueueUI() {
        fileTreeContainer.innerHTML = '';
        if (fileQueue.size === 0) {
            fileTreeContainer.innerHTML = '<p class="text-center text-gray-400 p-4">Nenhum arquivo na fila.</p>';
            return;
        }

        const files = Array.from(fileQueue);
        const basePath = origemInput.value || '';
        const tree = buildFileTree(files, basePath);
        renderTree(tree, fileTreeContainer, 0, basePath.split('/').pop());
    }

    function updateUIForState(state) {
        const isIdle = state === 'idle', isRunning = state === 'running', isPaused = state === 'paused',
              isStoppedOrCompleted = state === 'stopped' || state === 'completed', disableInputs = isRunning || isPaused;

        startBtn.classList.toggle('hidden', !isIdle && !isStoppedOrCompleted);
        pauseBtn.classList.toggle('hidden', !isRunning);
        resumeBtn.classList.toggle('hidden', !isPaused);
        stopBtn.classList.toggle('hidden', isIdle || isStoppedOrCompleted);
        
        [selectOrigemBtn, addFileBtn, clearQueueBtn, settingsBtn, origemInput].forEach(el => el.disabled = disableInputs);
        destinoInput.readOnly = disableInputs;
        selectDestinoBtn.disabled = disableInputs;
    }

    // --- Lógica de Atualização de Progresso (Feedback Visual) ---
    function updateProgress() {
        fetch('/get-progress').then(res => res.json()).then(data => {
            updateUIForState(data.status);
            window.individuallyPausedFiles = new Set(data.paused_individual_files);

            progressBarGeneral.style.width = `${data.progress_general}%`;
            progressTextGeneral.textContent = `Geral: ${Math.round(data.progress_general)}% (${data.files_processed}/${data.total_files}) | Decorrido: ${data.batch_elapsed_str}`;
            inProgressList.innerHTML = '';

            const activeFolders = new Set();
            Object.keys(data.files_in_progress).forEach(path => getFileParents(path).forEach(p => activeFolders.add(p)));

            // Atualiza lista "Em Progresso"
            Object.entries(data.files_in_progress).forEach(([path, info]) => {
                const isPaused = window.individuallyPausedFiles.has(path);
                const icon = isPaused ? 'fa-pause-circle text-yellow-500' : 'fa-cog fa-spin text-blue-500';
                const li = document.createElement('li');
                li.className = 'flex flex-col px-4 py-3 border-b border-gray-200';
                li.dataset.filepath = path;
                li.innerHTML = `
                    <div class="flex items-center gap-3">
                        <i class="fas ${icon}"></i>
                        <p class="flex-1 font-medium truncate" title="${path}">${info.filename}</p>
                        <span class="text-xs font-mono">${Math.round(info.progress || 0)}%</span>
                    </div>
                    <div class="flex items-center gap-2 mt-1 text-xs text-gray-500">
                        <span><i class='fas fa-clock mr-1'></i> ${info.elapsed_str || '--:--'}</span>
                        <span class="ml-auto"><i class='fas fa-hourglass-half mr-1'></i> ETA: ${info.eta_str || '--:--'}</span>
                    </div>
                    <div class="mt-2 h-1 bg-gray-200 rounded-full overflow-hidden">
                        <div class="h-1 ${isPaused ? 'bg-yellow-500' : 'bg-blue-600'} rounded-full" style="width: ${info.progress || 0}%;"></div>
                    </div>`;
                inProgressList.appendChild(li);

                const treeNodeIcon = fileTreeContainer.querySelector(`li[data-filepath="${CSS.escape(path)}"] .item-icon`);
                if (treeNodeIcon) treeNodeIcon.innerHTML = `<i class="fas ${icon} fa-fw"></i>`;
            });

            // Atualiza ícones na árvore
            fileTreeContainer.querySelectorAll('.item-icon, .folder-spinner').forEach(el => el.innerHTML = '');
            data.completed_files.forEach(f => {
                const iconEl = fileTreeContainer.querySelector(`li[data-filepath="${CSS.escape(f.source_path)}"] .item-icon`);
                if (iconEl) iconEl.innerHTML = '<i class="fas fa-check-circle text-green-600 fa-fw"></i>';
            });
            fileTreeContainer.querySelectorAll('.folder-item').forEach(folder => {
                const folderPath = folder.dataset.folderpath;
                const spinnerEl = folder.querySelector('.folder-spinner');
                if (activeFolders.has(folderPath) && spinnerEl) {
                    spinnerEl.innerHTML = '<i class="fas fa-cog fa-spin text-blue-500"></i>';
                }
            });

            // Popula lista de concluídos
            data.completed_files.forEach(fileInfo => {
                if (document.querySelector(`#completed-list li[data-filepath="${CSS.escape(fileInfo.output_path)}"]`)) return;
                const li = document.createElement('li');
                li.className = 'group relative flex items-center gap-3 px-4 py-3 hover:bg-gray-100';
                li.dataset.filepath = fileInfo.output_path;
                li.innerHTML = `<i class="fas fa-check-circle text-green-600"></i><p class="flex-1 font-medium truncate">${fileInfo.output_path.split(/[\\/]/).pop()}</p>`;
                completedList.prepend(li);
            });

            if (data.status === 'completed' || data.status === 'stopped') {
                clearInterval(progressInterval);
                progressInterval = null;
                // Final UI update
            }
        });
    }

    // --- Event Listeners ---
    selectOrigemBtn.addEventListener('click', async () => {
        const folderPath = await api.open_folder_dialog('Selecione a Pasta de Origem');
        if (folderPath) {
            origemInput.value = folderPath;
            const files = await api.scan_folder_recursively(folderPath);
            fileQueue = new Set(files);
            updateFileQueueUI();
        }
    });

    destinoInput.addEventListener('input', async () => {
        const path = destinoInput.value;
        destinoInput.classList.remove('border-green-500', 'border-red-500');
        if (path) {
            const isValid = await api.validate_path(path);
            destinoInput.classList.add(isValid ? 'border-green-500' : 'border-red-500');
        }
    });

    startBtn.addEventListener('click', () => { /* ...código sem alteração... */ });
    // ... outros listeners ...

    // --- Lógica do Tema Escuro ---
    function applyTheme(theme) {
        document.documentElement.classList.toggle('dark', theme === 'dark');
        darkModeToggle.checked = (theme === 'dark');
    }
    darkModeToggle.addEventListener('change', () => {
        const theme = darkModeToggle.checked ? 'dark' : 'light';
        localStorage.setItem('theme', theme);
        applyTheme(theme);
    });

    // --- Inicialização ---
    fetch('/get-settings').then(res => res.json()).then(data => {
        maxConcurrentInput.value = data.max_concurrent_tasks || 1;
    });
    applyTheme(localStorage.getItem('theme') || 'light');
    updateFileQueueUI();
    updateUIForState('idle');
});
```

-----

### **Conclusão e Próximos Passos**

Com as atualizações acima, todos os itens pendentes da Fase 1 foram abordados:

1.  **Feedback Visual (RNF3, RNF4):**

      * **Spinner em Pastas:** As pastas na fila de processamento agora exibem um spinner quando contêm arquivos que estão sendo processados.
      * **Pausa Individual:** A funcionalidade de pausar e retomar arquivos específicos pelo menu de contexto está totalmente implementada, do frontend ao backend. O estado visual (ícone de pausa/spinner, cor da barra) reflete essa mudança.

2.  **Tema Escuro (RNF3):**

      * **Consistência Visual:** O CSS foi aprimorado para garantir que o tema escuro seja aplicado de forma correta e esteticamente agradável a todos os componentes da interface, incluindo modais, menus de contexto, inputs e fontes.

**Para aplicar as mudanças, simplesmente substitua o conteúdo dos seus arquivos pelos códigos fornecidos.** O sistema agora deve apresentar uma interface mais robusta, informativa e visualmente coesa.

